import { Loading } from '../components';

const Demo = () => {
  return (
    <>
        <Loading loading = {false} />

        <div className={`
                bg-black 
                min-h-screen
                absolute  
                w-screen
            `}>h</div>
    </>
  )
}

export default Demo